'use client';

// Stub for Task 7 — renders nothing yet. Task 7 replaces this with the
// AI insights UI (generate/regenerate report, render markdown, etc.).
export function InsightsPanel({
  month,
  initialReport,
}: {
  month: string;
  initialReport: string | null;
}) {
  void month;
  void initialReport;
  return null;
}
